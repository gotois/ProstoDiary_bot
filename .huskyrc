{
  "hooks": {
    "pre-commit": "lint-staged",
    "pre-push": "npm run unit && npm run e2e:fast",
    "post-commit": "git update-index --again",
    "commit-msg": "commitlint -E HUSKY_GIT_PARAMS",
    "prepare-commit-msg": "exec < /dev/tty && git cz --hook"
  }
}
